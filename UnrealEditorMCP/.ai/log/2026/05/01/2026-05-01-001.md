# 操作日志：2026-05-01-001

## 操作类型
阶段 17 验收修复：apply_spec dispatch 补齐 + 文档同步

## P0：apply_spec dispatch 补齐

**问题**：阶段 17 命令式路径已完整（7 Handler + helpers + export_spec 扩展），但 `apply_spec` 的 `Execute` 方法缺少对新节点类型的调度分支，导致 spec 中 `type=node_by_class` / `variable_get` / `variable_set` 均触发 `UNSUPPORTED_TYPE` 错误。

**修复**（`MCPBlueprintSpecHandlers.cpp:431-489`）：
```cpp
else if (NodeSpec.Type == TEXT("node_by_class"))
{
    // 校验 node_class → ValidateNodeClass → CreateNodeByClass
}
else if (NodeSpec.Type == TEXT("variable_get") || NodeSpec.Type == TEXT("variable_set"))
{
    // 校验变量存在 → NewVariables 遍历 → CreateVariableGetNode/SetNode
}
```

新增 3 个 `else if` 分支，+60 行。

## P1：README.md 同步（4 处）

| 位置 | 旧值 | 新值 |
|------|------|------|
| 能力清单标题 | 58 Handler | 65 Handler |
| Blueprint 图编辑表 | 8 工具 | 15 工具（+add_node_by_class/var_node/set_pin_default/signature/disconnect/remove_node/remove_var） |
| 目录 Handler 计数 | 11 个领域文件（58 Handler） | 12 个领域文件（65 Handler） |
| 目录 tool_schemas | 58 tool schema | 65 tool schema |

## P2：tool_schemas.py + test_stage17.ps1

| 文件 | 变更 |
|------|------|
| `tool_schemas.py` | docstring 48→65 |
| `tests/test_stage17.ps1` | **新建** — 9 项验收（3 正向 + 5 错误 + 1 基础），使用单持久连接避免单客户端冲突 |

## 编译

```
4 actions, 0 errors, 29.77s
```

## 测试结果（9/9）

| # | 测试 | 结果 |
|---|------|------|
| 1 | create_bp | ✅ ok=True |
| 2 | apply_spec(node_by_class: K2Node_IfThenElse) | ✅ nodes=3 edges=1 |
| 3 | apply_spec(variable_set dispatch) | ✅ VARIABLE_NOT_FOUND（路由正确） |
| 4 | apply_spec(variable_get dispatch) | ✅ VARIABLE_NOT_FOUND（路由正确） |
| 5 | err-unsupported_type | ✅ UNSUPPORTED_TYPE |
| 6 | err-bad_node_class | ✅ INVALID_NODE_CLASS |
| 7 | err-var_not_found | ✅ VARIABLE_NOT_FOUND |
| 8 | err-missing_var_name | ✅ MISSING_PARAM |
| 9 | err-missing_node_class | ✅ MISSING_PARAM |

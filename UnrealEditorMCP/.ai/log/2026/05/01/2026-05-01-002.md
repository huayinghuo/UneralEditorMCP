# 操作日志：2026-05-01-002

## 操作类型
阶段 18：函数搜索 + PIE 运行时 + Enhanced Input 完整栈（65→87 Handler）

## 18A — 搜索 + CDO 默认值（+3, 65→68）

| Action | 类别 | 说明 |
|--------|------|------|
| `blueprint_search_functions` | Read | TObjectIterator 遍历所有 BlueprintCallable 函数，按 search_term 子串匹配 |
| `blueprint_set_variable_default` | Write | 加载 BP→GetDefaultObject→反射查找属性→ImportText 写入 CDO 默认值 |
| `blueprint_set_component_default` | Write | 遍历 SCS→查找组件模板→反射写入属性 |

**新增文件**：`MCPBlueprintUtilityHandlers.h/.cpp`

## 18B — PIE 运行时控制（+5, 68→73）

| Action | 类别 | 说明 |
|--------|------|------|
| `pie_start` | Write | GEditor->RequestPlaySession 启动 PIE |
| `pie_stop` | Write | GEditor->RequestEndPlayMap 停止 PIE |
| `pie_is_running` | Read | GEditor->IsPlaySessionInProgress 查询状态 |
| `get_actor_state` | Read | TActorIterator 在 PIE 世界中查找 Actor→返回位置/旋转/速度 |
| `set_level_default_pawn` | Write | GameMode->DefaultPawnClass 设置默认 Pawn |

**新增文件**：`MCPPIERuntimeHandlers.h/.cpp`

## 18C — Enhanced Input（+14, 73→87）

**InputAction（4）**：search/crate/get_info/delete
**InputMappingContext（8）**：search/create/get_info/delete/add_remove_mapping/set_action/set_key
**BP 节点（2）**：EnhancedInputComponent 事件/IMC 节点（通过 K2Node_CallFunction↔UEnhancedInputComponent）

**新增文件**：`MCPEnhancedInputHandlers.h/.cpp`
**Build.cs 修改**：新增 `EnhancedInput` / `InputCore` 模块依赖

**API 适配**：
- `UInputMappingContext::Mappings` → `GetMappings()`（UE 5.3 protected）
- `IAssetTools::DeleteAssets` → `ObjectTools::DeleteObjects`
- `K2Node_EnhancedInputAction.h` 不存在 → 改用 `UK2Node_CallFunction↔UEnhancedInputComponent`

## 跨阶段修复（4 项）

| 文件 | 问题 | 修复 |
|------|------|------|
| `MCPBlueprintHandlers.cpp:238` | `PC_Real` 无 subcategory→Kismet 编译器断言 | `PinSubCategory=PC_Float` |
| `MCPBlueprintGraphHelpers.cpp:117` | `AllocateDefaultPins` 先于 `AddNode` | 顺序修正+`PostPlacedNewNode` |
| `MCPBlueprintSpecHandlers.cpp:413` | 同上（call_function 分支） | 同上 |
| `MCPBlueprintGraphHandlers.cpp:259` | 同上（旧 add_call_function） | 同上 |

## 文档同步

| 文件 | 变更 |
|------|------|
| `plan.md` | Stage 18 ✅，65→87 Handler，Read 32/Write 54 |
| `README.md` | 87 Handler，新增 Enhanced Input 14 工具表，权限计数更新 |
| `AGENTS.md` | 87 Handler |
| `tool_schemas.py` | docstring 65→87 |

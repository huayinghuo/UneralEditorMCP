# 操作日志：2026-05-02-001

## 操作类型
阶段 19 + 20A：CDO 通用属性 + GameplayTag + Python 输出 + 超时警告（87→94 Handler）

## 19A — 父类放宽 + CDO 通用属性（2 Handler）

**父类检查**：`MCPBlueprintGraphHelpers.cpp:30` — `IsChildOf(AActor)` → `IsChildOf(UObject)` + `NotBlueprintable` 排除，支持创建 GAS AttributeSet 等非 Actor 父类 BP。

**CDO 属性**：`FindPropertyByName` 替代 `FindFProperty`（BP 变量在 UE 5.3 需此接口）。测试通过：`cdo_get`→val=0.0，`cdo_set`→val=999.0（read-back 确认）。

## 19B — GameplayTag 持久化（3 Handler）

`create_gameplay_tag`：`AddNativeGameplayTag` + `GConfig->SetArray` 写入 `DefaultGameplayTags.ini`（重启后 persistent）。

## 19C — Python 输出捕获

`ExecPythonCommand` → `ExecPythonCommandEx` + `CommandResult` + `LogOutput`（`PythonScriptTypes.h`）。

## 20A — EI 资产创建防弹窗 + 超时警告

**弹窗问题**：`CreatePackage` 在 UE 5.3 内建重名冲突检查无法通过任何前置 API（FindPackage/LoadPackage/FPaths::FileExists/GIsSilent）抑制。

**缓解措施**：
1. 三重检查（`static TSet<FString>` + `FindPackage` + `LoadPackage`）减少触发概率
2. Server 线程 5s 超时 → `TIMEOUT` 错误 + 清空队列（`MCPBridgeServer.cpp:395-408`）
3. Python `server.py` 翻译 `TIMEOUT` 为中文警告

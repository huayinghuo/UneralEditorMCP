# 操作日志：2026-05-04-002

## 操作类型
阶段 19 Bug 修复 + Handler 合并

## Bug #1：create_blueprint 截断

`MCPBlueprintHandlers.cpp:151` 在修改父类检查时遗留了 `return true;` 语句，导致 `create_blueprint` 在通过父类验证后直接返回，后续 BP 创建逻辑永不被执行。

**修复**：删除 `return true;`，使代码正常 fall-through 到 CreatePackage + FKismetEditorUtilities::CreateBlueprint。

## Bug #2：create_gameplay_tag ini 持久化

原实现使用 `GConfig->SetArray` + `GConfig->Flush(false, ConfigPath)` 写入 `DefaultGameplayTags.ini`。该文件在项目 `Config/` 目录中不存在且未被 GConfig 注册，导致 Flush 被忽略。

**修复**：改用 `FConfigFile::Read/GetString/SetString/Write` 直接文件 I/O：
1. `FPlatformFileManager` 检查文件存在性，不存在则创建目录+空文件
2. `FConfigFile::Read(ConfigPath)` 读入现有配置
3. `GetString` 检查 `GameplayTagList` 是否已含目标 TagEntry（`(Tag="x",DevComment="y")` 格式）
4. 去重后追加写入

## Handler 合并

`blueprint_create_actor_class`（`MCPBlueprintGraphHandlers`）与 `create_blueprint`（`MCPBlueprintHandlers`）在父类检查放开后功能完全重复（均支持任意 UObject 子类 + CreatePackage + FKismetEditorUtilities::CreateBlueprint + SavePackage）。

删除 `blueprint_create_actor_class`：
- `MCPBlueprintGraphHandlers.h:7-13` — 类声明
- `MCPBlueprintGraphHandlers.cpp:16-100` — Execute 实现（85 行）
- `MCPBridgeServer.cpp:86` — 注册
- `tool_schemas.py:325-337` — schema

95→94 Handler。编译 7 actions, 0 errors。

# 操作日志：2026-04-30-017

## 操作类型
阶段 10C 实现：Python 侧 `ue_get_mcp_config` 自举配置收敛

## 变更文件（4 个）

### 1. tool_schemas.py（+18 行）

新增 `BOOTSTRAP_MCP_CONFIG` 常量：
- `source = "python-bootstrap"`, `connected = False`
- `actions` 仅包含 `get_mcp_config`（最小可信集）
- docstring 补充自举配置说明

### 2. server.py — import（1 行）

```python
from .tool_schemas import TOOL_SCHEMAS, BOOTSTRAP_MCP_CONFIG
```

### 3. server.py — list_tools() 离线分支（~8 行）

旧逻辑：硬编码返回单个占位 tool
```python
return [types.Tool(name="ue_get_mcp_config", description="UE Editor not available...")]
```

新逻辑：从 `BOOTSTRAP_MCP_CONFIG["actions"]` 动态生成
```python
actions = BOOTSTRAP_MCP_CONFIG.get("actions", [])
for entry in actions:
    schema = TOOL_SCHEMAS.get(entry["action"])
    if schema: tools.append(types.Tool(**schema))
```

### 4. server.py — call_tool() 离线特判（+5 行）

```python
except ConnectionError as e:
    if name == "ue_get_mcp_config":
        return json.dumps(BOOTSTRAP_MCP_CONFIG)  # 返回结构化 bootstrap
    return ConnectionError  # 其他 tool 保持原行为
```

### 5. server.py — call_tool() 在线 enrichment（+4 行）

```python
if action == "get_mcp_config":
    response_data["source"] = "ue-live"
    response_data["connected"] = True
    response_data["schema_registry_count"] = len(TOOL_SCHEMAS)
```

## 验收测试

### 在线验证（UE Editor 运行中）

| 测试 | 方法 | 结果 |
|------|------|------|
| C++ `get_mcp_config` 联通 | 直连 TCP 9876 | ok=True, count=46, 6 个 11A action 可见 |
| Python `BOOTSTRAP_MCP_CONFIG` 导入 | UE Python exec | `source=python-bootstrap`, `connected=false` ✅ |
| Python `server.py` 语法 | UE py_compile | PARSE OK ✅ |
| `get_mcp_config` 存在于 TOOL_SCHEMAS | UE Python | True ✅ |

### 11A 回归测试

| 测试 | 结果 |
|------|------|
| ping → create_actor_bp → event_graph_info → add_event_node → add_call_function → connect_pins → compile_save | 10/10 通过 |
| create_bad_parent → bad_func_name → bad_guids | ok=False + 正确错误码 |

### 离线退化逻辑（静态验证）

| 场景 | list_tools() | call_tool("ue_get_mcp_config") | call_tool(其他) |
|------|-------------|-------------------------------|-----------------|
| UE 在线 | C++ action ∩ TOOL_SCHEMAS | C++ live + `source=ue-live` | 透传到 C++ |
| UE 断连/离线 | BOOTSTRAP_MCP_CONFIG["actions"] ∩ TOOL_SCHEMAS | `BOOTSTRAP_MCP_CONFIG` JSON | ConnectionError |

## 关键设计保证

1. **单一配置源**：`list_tools()` 离线 + `call_tool("ue_get_mcp_config")` 离线均派生自 `BOOTSTRAP_MCP_CONFIG`
2. **显式状态区分**：`source` 字段（`python-bootstrap` vs `ue-live`）+ `connected` 字段（true/false）
3. **不伪装能力**：bootstrap actions 仅 1 项（`get_mcp_config`），不复制 UE 全量 registry
4. **向后兼容**：非 `ue_get_mcp_config` 的离线调用行为不变

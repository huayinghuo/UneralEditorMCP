# 操作日志：2026-04-30-019

## 操作类型
10C / 11B 最小修复包

## 10C 修复（3 项）

| # | 问题 | 修复 | 文件:行 |
|---|------|------|---------|
| 1 | `call_tool()` 中 `arguments` 可能为 None，`dict(None)` 抛出 TypeError | `dict(arguments or {})` | `server.py:74` |
| 2 | `list_tools()` 仅在 `Exception` 时 fallback，UE 在线返回 ok=false 或 actions 缺失时不退化 | 改为 `if not actions: fallback` 统一 fallback 路径，消除 try/except 分支重复 | `server.py:33-64` |
| 3 | `BOOTSTRAP_MCP_CONFIG.count` 与 `len(actions)` 重复，导致离线/在线 count 语义不一致 | 移除 `count` 字段，由消费方 `len(actions)` 派生 | `tool_schemas.py` |

## 11B 修复（4 项）

| # | 问题 | 修复 | 文件:行 |
|---|------|------|---------|
| 4 | `MCPBlueprintSpecTypes.h` 缺少 `class UBlueprint;` 前向声明 | 新增前向声明 | `MCPBlueprintSpecTypes.h:6` |
| 5 | `blueprint_export_spec` 在无 EventGraph 时仍返回 `ok=true`（返回空 spec 但无错误） | 检查 OutErrorCode 是否为 GRAPH_NOT_FOUND，若是则 return false | `MCPBlueprintSpecHandlers.cpp:487-490` |
| 6 | `TryConnectPins()` 在 schema 拒绝连接时无结构化错误 | 新增 `CONNECTION_REJECTED` 错误码 + pin type 信息 | `MCPBlueprintGraphHelpers.cpp:209-215` |
| 7 | `apply_spec` 不自动 compile，文档未明确 | 响应新增 `compiled: false` 字段，README 标注"需后续 compile_save" | `MCPBlueprintSpecHandlers.cpp:453` |

## 文档修复

| 文件 | 变更 |
|------|------|
| `README.md` | Handler 46→48；新增 Blueprint 图编辑 8 tool + 10C 能力描述 |
| `plan.md` | round-trip 表述回退为"边格式兼容性验证通过" |
| `dev_last.md` | 重写为修复包摘要 |
| `plan_log.md` | 追加修复记录 |
| `log.md` | 追加 019 索引 |

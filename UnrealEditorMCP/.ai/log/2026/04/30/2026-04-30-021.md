# 操作日志：2026-04-30-021

## 操作类型

阶段 12A 实现：传输层稳态化与可观测性收口（8 文件变更，48→49 Handler，全量自测通过）

## Slice A — 状态面补齐

### 新增 `get_bridge_runtime_status` Handler（Read 类第 22 个，总计第 49 个）
- 声明：`Public/Handlers/MCPQueryHandlers.h`
- 实现：`Private/Handlers/MCPQueryHandlers.cpp`
- 注册：`MCPBridgeServer.cpp:50`
- Schema：`tool_schemas.py`

返回字段：`server_status` / `port` / `token_enabled` / `client_connected` / `last_error_code` / `last_error_message` / `transport_mode` / `bind_address`

### Server last error 追踪
- `MCPBridgeServer.h`：新增 `SetLastError()` / `GetLastErrorCode()` / `GetLastErrorMessage()` / `IsClientConnected()` / `GetServerPort()`
- `MCPBridgeServer.h`：新增 `LastErrorCode` / `LastErrorMessage` + `LastErrorCS` 字段
- `MCPBridgeServer.cpp`：所有 Run() 错误路径记录 last error（SOCKET_SUBSYSTEM_FAILED / CREATE_SOCKET_FAILED / BIND_FAILED / LISTEN_FAILED）
- 线程安全：网络线程写入，Game Thread 通过 `LastErrorCS` 读取

### 模块启动 warning 结构化
- `UnrealEditorMCPBridgeModule.cpp`：warning 日志含 `status=%d, port=%d, last_error=[%s] %s`

## Slice B — 单客户端语义固定

- `MCPBridgeServer.cpp` Run() 中 Accept 逻辑：
  - 无活跃客户端 → 正常 Accept
  - 已有活跃客户端 → Accept → 发送 `CLIENT_ALREADY_CONNECTED` 错误 → 关闭 Socket
- 日志：`Warning: Rejected second client connection from ...`

## Slice C — Python 错误诊断收口

### bridge_client.py
- 新增 `UEBridgeError(ConnectionError)` 基类，携带 `.code` 分类码
- 8 种错误分类：CONNECT_TIMEOUT / CONNECT_REFUSED / CONNECT_FAILED / READ_TIMEOUT / PEER_CLOSED / CLIENT_ALREADY_CONNECTED / RESPONSE_MISMATCH / CONNECTION_LOST
- `_read_response()` 检测 `CLIENT_ALREADY_CONNECTED` 响应并上抛
- `connect()` 区分 timeout / refused / OSError

### server.py
- `list_tools()` 在线失败时 warning 日志（含错误码）
- `call_tool()` 根据 `UEBridgeError.code` 映射到差异化用户可读提示
- bootstrap 降级契约不变（仅 `ue_get_mcp_config` 可离线返回）

## Slice D — 验收脚本与文档

- 新建 `tests/test_stage12a.ps1`：5 个测试域，27 项断言
- 更新 6 个文档文件：plan.md / plan_sync.md / plan_log.md / dev_last.md / AGENTS.md / README.md

## 编译修复

1. `GetLastErrorCode()` / `GetLastErrorMessage()` 去掉 const 限定符（FCriticalSection::Lock() 非 const）
2. `MCPQueryHandlers.cpp` 添加 `#include "Selection.h"`（非 unity build 暴露 USelection 未定义）

## 自测结果

| 项目 | 结果 |
|------|------|
| 编译 | 0 errors (4.97s) |
| ping | ok=True, pong ✅ |
| get_bridge_runtime_status | 8/8 字段正确 ✅ |
| 错误路径 | PARSE_ERROR / MISSING_ACTION / UNKNOWN_ACTION ✅ |
| 单客户端独占 | CLIENT_ALREADY_CONNECTED ✅ |
| 断连恢复 | Listening → Connected → Listening ✅ |
| test_stage12a.ps1 | 27/27 PASS, 0 FAIL ✅ |

## 变更文件清单（11 个文件）

| 文件 | 变更类型 |
|------|----------|
| `Public/MCPBridgeServer.h` | 新增字段/方法/注释 |
| `Private/MCPBridgeServer.cpp` | last error 记录 + 第二连接拒绝 + 新 Handler 注册 + const 修复 |
| `Public/Handlers/MCPQueryHandlers.h` | 新增 Handler 类声明 |
| `Private/Handlers/MCPQueryHandlers.cpp` | 新增 Handler 实现 + #include "Selection.h" 修复 |
| `Private/UnrealEditorMCPBridgeModule.cpp` | startup warning 结构化 |
| `Tools/.../bridge_client.py` | UEBridgeError 错误分类 |
| `Tools/.../server.py` | 日志增强 + 错误提示映射 |
| `Tools/.../tool_schemas.py` | 新增 schema |
| `Tools/.../tests/test_stage12a.ps1` | 新建验收脚本 |
| `AGENTS.md` | 新增 §2.6 运行时诊断 + Quick Reference 更新 |
| `README.md` | 单客户端描述增强 + 运行时诊断提及 |

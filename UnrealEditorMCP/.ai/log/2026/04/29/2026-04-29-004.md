# 操作日志：2026-04-29-004

## 操作类型
Handler 按领域拆分 + 新增只读命令 + 新增写命令

## Handler 拆分（从单文件到按领域）

### 删除
- `Public/MCPBridgeHandlers.h` — 原单文件 6 个 Handler 声明
- `Private/MCPBridgeHandlers.cpp` — 原单文件实现

### 新增（7 个文件）

| 文件 | 内容 |
|------|------|
| `Public/Handlers/MCPQueryHandlers.h` | Ping / EditorInfo / ProjectInfo / GetWorldState |
| `Public/Handlers/MCPActorHandlers.h` | GetSelectedActors / ListLevelActors / GetActorProperty |
| `Public/Handlers/MCPPythonHandler.h` | ExecutePython |
| `Public/Handlers/MCPWriteHandlers.h` | SpawnActor / SetTransform / SetProperty / SaveLevel |
| `Private/Handlers/MCPQueryHandlers.cpp` | 实现含 get_world_state |
| `Private/Handlers/MCPActorHandlers.cpp` | 实现含 level_get_actor_property（UE 反射 ExportText） |
| `Private/Handlers/MCPWriteHandlers.cpp` | 实现含 spawn / transform / property / save |

## MCPBridgeHelpers 增强

新增函数（从 3 个 → 7 个）：

```
GetEditorWorld()
FindActorByNameOrLabel(World, Name)          ← 新增
BuildActorJson(Actor, bIncludeTransform)
GetActorPropertyAsString(Actor, PropertyName)  ← 新增
SetActorPropertyFromString(Actor, Prop, Val)   ← 新增
SpawnActorInEditor(World, ClassName, Loc, Rot, Scale) ← 新增
ParseVectorOptional(Payload, Key, Default)     ← 新增
BuildErrorResponse(Code, Message)
```

## MCPBridgeServer 变更
- 构造函数 `RegisterHandlers()` 注册数 6 → 12
- `#include` 路径从旧 `MCPBridgeHandlers.h` 改为 4 个新 Handler 文件

## Python MCP Server 变更
- tool 列表 6 → 12
- `call_tool` 新增 payload 提取：spawn_actor / level_set_actor_transform / actor_set_property
- ue_spawn_actor 支持 class_name + 可选 location/rotation/scale
- ue_level_set_actor_transform 支持 actor_name + 可选 location/rotation/scale
- ue_actor_set_property 支持 actor_name + property_name + value

## 编译结果
- 编译通过（0 警告 0 错误）
- 修复 `ExportTextItem`→`ExportTextItem_Direct` 弃用警告
- 修复 `ExportTextItem_InContainer` 参数签名不匹配

## 测试结果

### 只读命令
```
get_world_state:              ✅ {actor_count:79, has_world_partition:true}
level_get_actor_property:     ✅ bHidden="False" (found=true)
  不存在的属性:                 ✅ found=false
  不存在的 Actor:               ✅ ACTOR_NOT_FOUND
```

### 写命令
```
spawn_actor(PointLight):                  ✅ 生成于原点
spawn_actor(StaticMeshActor, location):   ✅ 生成于指定位置
level_set_actor_transform(location/scale):✅ 位置/缩放更新成功
actor_set_property(bHidden=True):         ✅ 写入 + read-back 验证通过
save_current_level:                       ⚠️ 临时关卡无文件路径（预期行为）
```

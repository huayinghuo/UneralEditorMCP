[project]
name = "mcp-bridge-server"
version = "0.1.0"
description = "MCP Bridge Server for Unreal Editor"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
mcp-bridge-server = "mcp_bridge_server.server:main"
